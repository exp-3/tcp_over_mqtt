{
  "name": "tcp_over_mqtt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "mqtt": "^5.14.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
