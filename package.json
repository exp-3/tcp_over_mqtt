{
  "name": "tcp_over_mqtt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts",
    "check": "tsc --noEmit",
    "test": "bun test",
    "verify": "bun run check && bun test",
    "build": "bun run scripts/build.ts native",
    "build:linux-x64": "bun run scripts/build.ts linux-x64",
    "build:linux-arm64": "bun run scripts/build.ts linux-arm64",
    "build:windows-x64": "bun run scripts/build.ts windows-x64",
    "clean": "bun run scripts/build.ts clean"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1",
    "mqtt": "^5.14.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
